import express from "express";

const router = express.Router();

// Register service handlers here once they are implemented. Empty Express
// route declarations crash the application during startup.

export default router;
